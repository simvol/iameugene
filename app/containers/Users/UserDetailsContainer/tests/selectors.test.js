// import { selectUserDetailsContainerDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectUserDetailsContainerDomain();

describe('selectUserDetailsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
