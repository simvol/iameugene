import expect from 'expect';
import userDetailsContainerReducer from '../reducer';
import { fromJS } from 'immutable';

describe('userDetailsContainerReducer', () => {
  it('returns the initial state', () => {
    expect(true).toEqual(true);
  });
});
