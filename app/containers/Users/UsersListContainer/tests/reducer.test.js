import expect from 'expect';
import usersListContainerReducer from '../reducer';
import { fromJS } from 'immutable';

describe('usersListContainerReducer', () => {
  it('returns the initial state', () => {
    expect(true).toEqual(true);
  });
});
