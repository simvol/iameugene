// import { selectApplicationsListContainerDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectApplicationsListContainerDomain();

describe('selectApplicationsListContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
