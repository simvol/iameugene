import expect from 'expect';
import applicationsListContainerReducer from '../reducer';
import { fromJS } from 'immutable';

describe('applicationsListContainerReducer', () => {
  it('returns the initial state', () => {
    expect(true).toEqual(true);  
  });
});
