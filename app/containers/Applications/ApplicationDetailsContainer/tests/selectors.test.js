// import { selectApplicationDetailsContainerDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectApplicationDetailsContainerDomain();

describe('selectApplicationDetailsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false);
  });
});
